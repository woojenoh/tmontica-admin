export interface IStatMenu {
  menuId: number;
  menuName: string;
  totalPrice: number;
}

export interface IStatMenuData {
  label: string;
  backgroundColor: string;
  hoverBackgroundColor: string;
  data: number[];
}
